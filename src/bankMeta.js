/**
 * Bank metadata — single source of truth for curriculum sizing.
 * generatedBank.js and briggsProblems both re-export from here so filters
 * and the generator cannot drift on QUESTIONS_PER_TOPIC.
 */

export const QUESTIONS_PER_TOPIC = 50;
